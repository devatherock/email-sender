services:
  - type: web
    name: email-sender
    env: docker
    plan: free
    autoDeploy: false
    dockerfilePath: ./render/Dockerfile
    dockerContext: ./render
    envVars:
      - key: PORT
        property: port